{
  "name": "typescript",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "i18next": "^22.4.13",
    "i18next-fs-backend": "^2.1.1",
    "i18next-http-backend": "^2.2.0",
    "typescript": "^4.9.5",
    "vite": "^4.3.4"
  },
  "scripts": {
    "compile": "tsc --build",
    "build": "npm run compile && vite build --mode production",
    "copy": "mkdir dist/public && cp public/style.css dist/public/style.css",
    "serve": "vite preview"
  }
}
